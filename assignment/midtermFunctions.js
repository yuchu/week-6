/* ================================
Week 6 Assignment: Midterm Function Signatures
================================ */
//Filter stations by weekday_type(weekday/weekend) & cluster number
var ClusterFilter = function(stations, weekday_type , cluster_number){return cluster_stations;};

//Find the most imbalanced station
var ImbalanceFilter = function(stations){return imbalanced_stations;};

//Find the most & least active stations
var ActivityFilter = function(stations){return active_stations;};
